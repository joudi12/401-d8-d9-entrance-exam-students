<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/style.css">
    <title>Document</title>
</head>

<body>
    <header>
        <a href="/home">Home</a>
        <a href="/my-characters">my-characters</a>
    </header>
    <main>
        <div class="main">
            <%result.forEach(element=>{%>
            <div class="charecter">
                <img src="<%= element.image%>" alt="" width="100px" height="100px">
                <h2> NAME:<%= element.name%></h2>
                <h3>Patronus:<%= element.patronus%></h3>
                <h4>Alive:<%= element.alive%></h4>

                <form action="/my-characters" method="POST">
                    <input type="text" name="image" value="<%= element.image%>" style="display: none;">
                    <input type="text" name="name" value="<%= element.name%>" style="display: none;">
                    <input type="text" name="patronus" value="<%= element.patronus%>" style="display: none;">
                    <input type="text" name="alive" value="<%= element.alive%>" style="display: none;">
                    <button type="submit">SAVE CHARACTERS</button>
                </form>
            </div>
            <%})%>
        </div>
    </main>
</body>

</html>